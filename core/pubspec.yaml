name: wealth_core
description: >-
  Pure-Dart core of Wealth OS — money, units, dates, ledger & financial
  domain logic. No Flutter dependency. Fully unit-testable with dart test.
version: 0.1.0
publish_to: "none"

environment:
  sdk: ">=3.3.0 <4.0.0"

dev_dependencies:
  test: ^1.25.0
